class AppSizes {
  // ── Radius Scales ──
  static const double radiusSm = 4.0; // Badges, tags
  static const double radiusMd = 8.0; // Inputs, buttons
  static const double radiusLg = 16.0; // Cards, panels, modals
  static const double radiusXl = 20.0; // Large container hero layouts

  // ── Card Configurations ──
  static const double cardRadius = radiusLg; // 14px layout matching HTML design

  // ── Button Configurations ──
  static const double buttonHeightSm =
      28.0; // Small variant (7px padding padding equivalents)
  static const double buttonHeight =
      40.0; // Medium default variant (10px padding equivalents)
  static const double buttonHeightLg =
      48.0; // Large variant (13px padding equivalents)

  // ── Input Field Configurations ──
  static const double inputHeight = 40.0; // Standard technical UI input scale

  // ── Avatar Configurations ──
  static const double avatarRadius = 18.0;

  // ── Icon Sizes ──
  static const double iconLg = 24.0;
  static const double iconMd = 18.0;
  static const double iconSm = 14.0;
}
